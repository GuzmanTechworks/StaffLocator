import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ph.gov.cagayan.stafflocator',
  appName: 'ISD STAFF LOCATOR',
  webDir: 'www',

  // plugins: {
  //   CapacitorHttp: {
  //     enabled: true,
  //   },
  // },
};

export default config;

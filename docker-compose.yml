services:
  api:
    build:
      context: ./server
    env_file:
      - ./server/.env
    restart: unless-stopped
    expose:
      - "3000"

  web:
    build:
      context: .
    depends_on:
      - api
    ports:
      - "8080:80"
    restart: unless-stopped